# Long-Running State

- **Goal:** One cumulative Kairos Midnight Preprod product for Levels 1–5.
- **Decision:** One contract contains the eight-position private signal market, public seven-day round deadlines with a one-day resolution window and missed-round expiry, fixed three-token genesis, public NIGHT/quote routes with asymmetric fees and bounded KAI incentives. Resolution or expiry atomically reapportions internal NIGHT reserves; a separate repair call handles later trades. A new round requires the current reserve split to match the target. It has no autonomous submission or external liquidity venue; see `docs/research/PRODUCT_CAPABILITIES.md`.
- **Verified locally:** Compact compiler 0.31.1 compiled eight circuits; eleven contract tests passed. The wallet connector discovers Lace under an opaque provider key and reports Lace's network mismatch. The UI and provider wrappers expose preparation, proving, balancing, submission, and finalization stages, including the submitted transaction ID while finalization is pending; stage-specific failure copy was exercised with a browser mock. Twenty-five application unit tests, four read-only transaction verifier tests, lint, typecheck, production build, and six Playwright flows passed locally. Contract tests simulate settled token balances between calls. Proof server 8.1.0 at `127.0.0.1:6301` checked and generated proofs for all eight exported circuits with synthetic inputs via `npm run proof:smoke`; no transaction was balanced or submitted.
- **Preprod evidence:** The owner's browser exposes Lace under an opaque key with `rdns: io.lace.wallet` and connector API 4.0.1. After selecting Preprod in Lace, the owner confirmed Kairos connected and displayed an address. This confirms wallet connection only. An older Preprod deployment at `89001d3f0ddb1b1d9e30fb1fa34f3c26658f5ba20bb6c25870e18dac5bbba8e4` was checked against the current artifact: all eight current verifier keys failed to match its seven older circuits. It cannot serve as the current deployment. No finalized deployment or circuit transaction of the current build has been observed. The Codex in-app browser has no injected Lace.
- **External blockers:** No verified organizer/category approval, public demo URL, hosted CI run/badge, or real users/feedback. Levels 4–5 challenge completion depend on these conditions. Manual exclusions: Vercel deployment, video, screenshots, and collecting feedback.
- **Next:** Obtain a finalized Preprod deployment of the current eight-circuit artifact and public state from the owner's Lace-enabled browser. Run `npm run verify:preprod -- <address> <tx-id>` against the live indexer; its nonexistent-address and mismatched-artifact failure paths have been exercised, but no current deployment has been verified. A separate read-only `verify:activity` command checks a finalized circuit transaction against an exact address and entry point; it has deterministic tests and a live indexer query-shape check against unrelated public activity, but no current Kairos transaction to verify yet. Then separately verify issuance, trade, and reserve changes before updating the README address or runtime claims. The Codex browser inventory has only an in-app browser without Lace.
